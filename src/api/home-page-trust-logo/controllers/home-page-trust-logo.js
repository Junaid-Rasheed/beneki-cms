'use strict';

/**
 * home-page-trust-logo controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::home-page-trust-logo.home-page-trust-logo');
